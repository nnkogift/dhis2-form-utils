node_modules
dist
coverage
storybook-static
pnpm-lock.yaml
*.min.js
.d2

# Design handoff reference material (prototypes, exported design-system bundles) —
# not application source.
apps/playground/design

# JSONC (fallow writes .fallowrc.json with comments; both extensions support them).
.fallowrc.json