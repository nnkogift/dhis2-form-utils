{
  "$schema": "https://raw.githubusercontent.com/fallow-rs/fallow/main/schema.json",
  "entry": [
    "src/index.{ts,tsx,js,jsx}",
    "src/main.{ts,tsx,js,jsx}",
    "apps/playground/src/AppWrapper.tsx"
  ],
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*",
      "utils/*",
      "components/*"
    ]
  },
  "duplicates": {
    // Hide pair-only clones; focus on widespread copy-paste
    // worth refactoring. Lower to 2 to report every duplicate pair.
    "minOccurrences": 3,
    "ignore": [
      // Storybook stories are deliberately duplicated one file per UI adapter
      // (dhis2-ui/mantine/mui) for parity and easy side-by-side comparison.
      "apps/storybook/stories/**"
    ]
  },
  "rules": {
    "unused-dependencies": "warn"
  },
  "ignoreDependencies": [
    // Used only via subpath imports (./coordinateValue, ./geojsonValue) and a
    // side-effect CSS import (./style.css) in dhis2-ui/mantine/mui/hooks —
    // fallow's dependency-usage detector doesn't resolve those import forms.
    "@nnkogift/dhis2-form-utils-map"
  ]
}
