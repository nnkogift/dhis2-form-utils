---
'@nnkogift/dhis2-form-utils-devtools': minor
---

Add an in-scope/all segmented control to the Rules panel header, so the Rules tab can be filtered down to only the current stage's rules (default) or show every rule in the program.
